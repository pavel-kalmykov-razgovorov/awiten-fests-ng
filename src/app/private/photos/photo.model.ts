export class Photo {
  public id: number;
  public name: string;
  public url: string;

  constructor(id: number, location: string, url: string) {
    this.id = id;
    this.name = name;
    this.url = url;
  }
}
